# This file specifies the dependencies that would be needed by another project that depends on this
# project. Specifically, this file is fed into `install_requires` in `setup.py`. This file should
# contain ranges of requirements, not pinned versions. When you update this file, run
# `tox -e pip-compile` to automatically update `requirements.txt`.
#
# INSERT REQUIREMENTS HERE
click
flask
flask_sqlalchemy
requests
